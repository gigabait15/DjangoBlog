from django.urls import path
from Blog.apps import BlogConfig

app_name = BlogConfig.name


urlpatterns = [
    path(...,...,name=''),
    path(...,...,name=''),
    path(...,...,name=''),
    path(...,...,name=''),
]